# AIガイド

`zenn.dev` の記事を管理する。

## 連携ガイド

- https://zenn.dev/zenn/articles/connect-to-github

## ディレクトリ構造

- `articles/` - 記事一覧

## 開発コマンド

- `bun run new:article <slug>` - slug名の記事を作成する
- `bun run preview` - 開発サーバーを起動しプレビューする
- `bun run lint <file>` - 対象のファイルに対してtextlintを実行する

## 記述ルール

記事を書く・直す・レビューするときは、先に対象ファイルと直近の `articles/` を2本読む。語尾、密度、語り口は既存記事に合わせる。

`/japanese-tech-writing` は論証の穴、冗長、LLM句の点検に使う。一文一行や「である」調などの整形で、既存の書き味を上書きしない。

- 自分の設定と判断を見せる。教科書の解説にしない
- 語尾は「している」「ことにした」「嬉しい」「便利」。文末の「である。」を増やさない
- ですます調にしない
- 理由は一段だけ。表や対応表で膨らませない
- URL は単独行。補足リンクは脚注でよい
- 既存の見出し（英語含む）を、規範に合わせて書き換えない
- 推敲では語尾とリズムを「整えない」。直すのは事実の誤りと論の穴

## Skills

- `/japanese-tech-writing` - 論証の穴、冗長、LLM句を点検する。書き味の上書きには使わない
