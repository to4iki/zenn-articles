---
title: "git + gh の環境設定"
emoji: "💻"
type: "tech"
topics:
  - "git"
  - "gh"
published: true
published_at: "2021-04-07 03:26"
---

自身のgit関連の設定 
- [gitconfig](https://github.com/to4iki/dotfiles/blob/master/git/gitconfig)
- [gh/config.yml](https://github.com/to4iki/dotfiles/blob/master/gh/config.yml)
- [tigrc](https://github.com/to4iki/dotfiles/blob/master/etc/tigrc)

に関してまとめる

## git
alias

```shell
br = branch --verbose --verbose --color --sort=-authordate
co = checkout
d = diff --histogram
f = fetch
l = log --oneline
n = now --all --stat
s = status -sb
undo = reset --soft HEAD~
```

スナップショット用途でこまめにcommitして `git rebase -i` でまとめることが多いので、git-nowを利用している
https://github.com/iwata/git-now

```
$ git n
[master 351631f] [from now] 2021/04/06 22:31:19
1 file changed, 1 insertion(+), 1 deletion(-)
```

## gh
以前までは [hub](https://github.com/github/hub) を利用していたが、公式の GitHub CLI に移行することにした
https://cli.github.com/manual/

※ 該当のコミットを表示する `hub browse` 差分表示用の `hub compare` の代替が `gh` コマンドには無いが許容する

主な用途はissue/PRの閲覧、作成、 `hub` よりも出来ることは少ないがCUIでissue/PRを作る時のインターフェースが丁寧だったりブラウザでプレビュー出来るのが良い

#### repo
alias: `gh o`
```shell
# カレントディレクトリのリポジトリ内容の閲覧
$ gh repo view --web
```

#### pr
alias: `gh co`
```shell
# プルリクエストのブランチへのチェックアウト
$ gh pr checkout {<number> | <url> | <branch>}
```

---

```shell
# 自分の作成したプルリクエストの一覧をインタラクティブ検索
$ gh pr list -s all -a <自分のアカウント名> | fzf
```

```shell
# PRを作成しpush後に自動的にブラウザで開く
$ gh pr create -w
```

#### issue
```shell
# 自分の作成したイシューの一覧をインタラクティブ検索
$ gh issue list -s all -a <自分のアカウント名> | fzf
```

#### api
`gh pr view [<number> | <url> | <branch>] [flags]` ではでコミットハッシュからPRを見ることができないが、[GitHub GraphQL API](https://docs.github.com/en/graphql)を実行することで対応可能[^1]
[^1]: https://blog.agile.esm.co.jp/entry/2021/01/29/171120

alias:
```shell
# 任意のコミットハッシュから対応するPRの番号を返す
$ gh searchpr <コミットハッシュ>

# 任意のコミットハッシュから対応するPRをブラウザで表示する
$ gh openpr <コミットハッシュ>
```

## tig
key-binding:
```shell
# P: 任意のコミットハッシュから対応するPRをブラウザで表示する
bind main P !@gh openpr %(commit)
bind blame P !@gh openpr %(commit)

# R: 選択中のcommitを基点にrebaseする
bind main R !git rebase -i %(commit)
```